# Leave empty for the explicitly labelled, deterministic demo mode.
# Set to your backend endpoint, e.g. http://localhost:8000/api/brief/analyze.
# The backend must allow this frontend origin via CORS if hosted separately.
# Never expose an AI provider secret through a NEXT_PUBLIC variable.
NEXT_PUBLIC_BRIEF_API_URL=
